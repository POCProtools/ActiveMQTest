apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-famille-test-controller
spec:
  ports:
    - name: http
      targetPort: 8080
      port: 8080
  selector:
    app: rabbitmq-famille-test-controller
